server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/note_note?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: rui  # 你的数据库用户名
    password: 124503  # 你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver

# 通义千问API配置
tongyi:
  api:
    key: ${TONGYI_API_KEY:sk-5f4a6d038b09420fa79b4f6eea521a83}  # 请在此处填写你的通义千问API密钥
    base-url: https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
    vision-url: https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation

mybatis:
  mapper-locations: classpath:mapper/*.xml  # MyBatis XML映射文件位置
  type-aliases-package: com.shop.entity  # 实体类包路径
  configuration:
    map-underscore-to-camel-case: true  # 开启驼峰命名自动映射

logging:
  level:
    com:
      shop:
        mapper: DEBUG